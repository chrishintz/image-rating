<% @photos.each do |pic| %>
  <div class="picture">
    <%= link_to image_tag("#{pic.photo_url.url(:square)}") %>
  </div>
  <div class="caption">
    <p> <%= pic.description %></p>
  </div>
<% end %>
