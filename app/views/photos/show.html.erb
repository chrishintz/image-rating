<% @photos.each do |pic| %>
  <div class="picture">
    <%= link_to image_tag("#{pic.photo_url}", size: "320x320") %>
  </div>
  <div class="caption">
    <p> <%= pic.description %></p>
  </div>
<% end %>
